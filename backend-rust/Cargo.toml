[package]
name = "ai-bid"
version = "0.1.0"
edition = "2024"
default-run = "ai-bid"

[dependencies]
anyhow = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
uuid = { version = "1", features = ["v4", "v5"] }
pdfplumber = { version = "0.3", features = ["serde"] }
pdf-extract = "0.12"
rayon = "1"
regex = "1"
fastembed = { version = "5", features = ["ort-load-dynamic"] }
serde_yaml = "0.9"
tokio = { version = "1", features = ["full"] }
reqwest = { version = "0.12", features = ["json", "stream"] }
ureq = "3"
chrono = "0.4"
async-trait = "0.1"
dotenv = "0.15"
axum = { version = "0.7", features = ["multipart", "macros"] }
tower-http = { version = "0.5", features = ["cors"] }
tower = { version = "0.5", features = ["util"] }
tokio-stream = "0.1"
async-stream = "0.3"
futures-core = "0.3"
utoipa = { version = "4", features = ["axum_extras", "chrono"] }
# Qdrant 向量库客户端（gRPC）。版本用 1.x 最新，builder API 从 1.8 起可用
qdrant-client = "1"
hmac = "0.12"
sha2 = "0.10"
hex = "0.4.3"
neo4rs = "0.8.0"

[build-dependencies]
cc = "1"

[dev-dependencies]
tempfile = "3"
