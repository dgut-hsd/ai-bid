# 知识沉淀与 Neo4j 模块 — 质量指标验证结果（卓茵）

## 1. 单元测试覆盖率与结果
- **执行命令**：`cargo test --lib knowledge -- --nocapture`
- **测试结果**：**26 passed**, 0 failed, 0 ignored (通过率 100%)
- **覆盖核心逻辑**：
  - `knowledge::collect`：候选抽取逻辑、查重与过滤规则
  - `knowledge::extract`：中文数字转换、脏格式去重、确定性 ID 生成、元数据解析

## 2. 金标集 (Golden Set) 评价指标统计
- **样本数据量**：从 `candidates.jsonl` 中抽取并人工标注 30 条真实 `legal_basis` 样本。
- **金标集文件位置**：`docs/工作成果报告提交/golden_set.csv`

| 质量评价指标 | 目标值 | 实测结果 | 结论 |
| :--- | :---: | :---: | :---: |
| **法律名称规范化准确率** | ≥ 95% | **100%** | 达标 (成功去除 URL/脏字符并补全标准法名) |
| **条款号规范化准确率** | ≥ 95% | **100%** | 达标 (中文数字全角统一归一化，如“第二十条”→“第20条”) |
| **实体抽取准确率** | ≥ 90% | **96.7%** | 达标 (法律名与条款号结构化拆分精准) |
| **法律元数据解析准确率** | ≥ 90% | **96.7%** | 达标 (效力层级、文号等元数据识别无误) |
| **同一法律不同写法收敛率** | 100% | **100%** | 达标 (不同脏格式均准确映射至稳定 `law_id`) |

## 3. 边界与异常用例测试
1. **空输入测试**：`legal_basis` 为空数组 `[]` 时，系统正常跳过，未产生无效节点或报错。
2. **中危无依据过滤**：中危风险且无 `legal_basis` 的条目被 `collect_candidates` 正常过滤，防止垃圾数据入库。